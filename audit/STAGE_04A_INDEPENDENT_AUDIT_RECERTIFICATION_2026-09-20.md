# Stage 4A — Independent-Audit Partial Reopening and Re-certification

Date: **2026-09-20**  
Trigger checkpoint: `5bdf156347da4267167513d96b5ef53456cd3f9a`  
Repair branch: `audit/full-equilibrium-correspondence`

## 1. Reason for reopening

An external clean-room audit found:

1. C7 / manuscript Proposition 2 omitted the sourcing-cap condition needed for a **positive-length feasible** (+2) best-response segment;
2. C8 / manuscript Proposition 3 had a correct five-case statement but an under-documented completeness proof;
3. prior mechanism prose overstated downstream exit as necessary for multiplicity.

The original Stage-4A PASS remains historical provenance. This document is the current amendment/re-certification record.

## 2. C7 independent re-certification

Normalize (delta=D/H) and (ho=b/H^2). The uncapped pure global response contains

[
U(y)=delta+2y
]

for (4/9<ho<2/3) on the relevant rival-exit subinterval.

The constrained response is

[
B_phi(y)=min{phi,R(y)}.
]

For (1/2leho<2/3), the uncapped exit interval starts at (y=0). Its feasible part has positive length iff

[
phi>delta.
]

At (phi=delta) it degenerates to (y=0).

For (4/9<ho<1/2), the exit interval starts at

[
y_M=rac{delta(1-2ho)}{4ho-1},
]

and

[
U(y_M)=M=rac{delta}{4ho-1}.
]

Hence a positive-length feasible exit segment survives iff

[
phi>M.
]

At (phi=M) it degenerates to the join point.

The counterexample to the old over-quantified wording is

[
(ho,delta,phi)=left(rac35,1,rac1{10}ight),
]

for which (B_phi(y)equiv1/10) on the entire feasible rival interval.

A source-feasible primitive realization is

[
H=gamma=1,qquad b=rac35,qquad
phi=rac1{10},qquad a=rac{221}{200},
]

giving (D=1) and satisfying (b>(2H/3)^2).

The original large-cap witness

[
(ho,delta,phi)=left(rac35,1,2ight)
]

still has a nondegenerate (+2) segment. Therefore the correct C7 conclusion is existential in the cap:

> admissible source parameters exist for which the constrained pure global sourcing response is increasing on a nondegenerate interval, so the published Proposition 5 cannot be a global strategic-substitutes theorem.

### C7 verdict

[
oxed{	extbf{PASS — AMENDED QUANTIFIER}}
]

No Stage-7.5A rollback is required: the existing Stage-7.5A claim-scope ledger already said “whenever the rival-exit piece is feasible.” The regression occurred in the later Stage-4A/Stage-8/manuscript wording chain.

## 3. C8 proof-completeness re-certification

The five-case equilibrium set was independently re-derived and retained.

### (ho>2/3)

The capped response is continuous and globally Lipschitz with constant

[
k=rac{2}{9ho-4}<1.
]

Thus

[
T(x,y)=(B_phi(y),B_phi(x))
]

is a contraction of ([0,phi]^2). The fixed point is unique, and symmetry of (T) forces it to be symmetric.

### (ho=2/3)

[
B_phi(y)=min{phi,max{0,delta-y}}.
]

Hence:

- (phi<delta/2): only ((phi,phi));
- (phi=delta/2): singleton ((delta/2,delta/2));
- (phi>delta/2): the nondegenerate segment (x+y=delta) clipped to the box.

### (4/9<ho<2/3)

Let

[
s=rac{2delta}{9ho-2}.
]

The independently checked identities imply

[
y_A<s<delta
]

and the crossing property

[
R(z)>zquad(z<s),qquad R(s)=s,qquad R(z)<zquad(z>s).
]

If (phile s), any hypothetical asymmetric mutual response contradicts this crossing property, leaving only ((phi,phi)).

If (phi>s), any asymmetric equilibrium can be ordered (x_H>x_L), and necessarily

[
x_L<s<x_H.
]

The proof is exhaustive after splitting:

- (x_H<delta): the low action is on the active branch; an uncapped high action cannot come from active-active, exit, monopoly, or zero branches, so the high action must be the cap (x_H=phi), with (x_L=A(phi));
- (x_Hgedelta): the low action is (x_L=0), and the high action is (x_H=B_phi(0)=min{phi,R(0)}).

These cases combine to

[
x_H=min{phi,R(0)},qquad x_L=R(x_H),
]

with exactly one ordered asymmetric pair and its mirror, plus ((s,s)).

The manuscript Appendix now contains this exclusion logic explicitly rather than replacing it with a branch-intersection assertion.

### C8 verdict

[
oxed{	extbf{PASS — PROOF COMPLETENESS RECERTIFIED}}
]

No theorem-set change was required.

## 4. Multiplicity mechanism correction

The statement “multiplicity is generated by downstream exit” is rejected.

At

[
(ho,delta,phi)=left(rac35,1,rac34ight),
]

for all feasible (x,yin[0,3/4]),

[
delta+2x-ygerac14,qquad
delta+2y-xgerac14,
]

so both downstream firms remain active after every sourcing history. Yet the pure equilibria are

[
left(rac{10}{17},rac{10}{17}ight),qquad
left(rac34,rac5{14}ight),qquad
left(rac5{14},rac34ight).
]

Thus:

- own-payoff strict concavity does not imply game-level uniqueness;
- exit creates the C7 positive-slope branch;
- exit is not necessary for C8 multiplicity;
- in the all-active example, the steep decreasing active best response plus the cap supports the asymmetric pair.

## 5. Supporting proof repairs

The manuscript Appendix now also makes explicit:

- coercivity of the Stage-II potential before claiming existence;
- finite active-set transitions, one-time own entry, no rival re-entry, and simultaneous-exit derivative jumps for global own-payoff concavity;
- the literal full-coverage Hotelling global pure-price best response;
- no-loss root domain, full-market branch location, strict-gain/tie boundaries, and the (1/90) regression.

## 6. Independent verification artifact

`code/independent_audit_repair_verify.py` reconstructs Cournot payoffs from primitive nonnegative-output active sets and does **not** import the production best-response map.

It permanently checks:

- C7 small-cap branch erasure and exact threshold crossings;
- (ho=1/2), (ho=2/3), (phi=s), (phi=delta), (phi=R(0));
- representative low-(ho) (M) boundaries;
- all-active multiplicity;
- literal Hotelling interior/boundary/corner mutual best responses;
- weak-dominance facts;
- no-loss strict gain and tie;
- welfare regressions.

Finite parameter checks are retained as regression evidence only; the analytic C8 completeness proof is in the Appendix.

## 7. Formal-verification boundary

The Lean source was not changed. The certified blobs remain:

- `ShyStenbackaFormal/Stage075A.lean`: `a634b04eb2ec13a632f10928974f8eab96d1d05c`;
- root: `e4a44acff03704dbf427b792d9528cf9a0a3c602`;
- `lakefile.toml`: `6669c6b90caf3c05912556109ead89c33400fe6c`;
- `lean-toolchain`: `7aca1d8a939cc24c413eddd793671cdce7070b74`;
- Lean workflow: `886c142e65d35d746cb1803e266aa0d687f7514e`.

The existing formal statement-fidelity map already says that the C7 Lean theorems certify join identities and slope (2>0), **not** global optimality or cap feasibility. C8 remains analytically, not formally, certified.

## 8. Stage-4A re-certification verdict

[
oxed{	extbf{GO — MATHEMATICAL ADVERSARIAL CERTIFICATION PASS, AMENDED}}
]

The original Stage-4A checkpoint is not erased. This amendment supersedes its C7 cap wording and completes the C8 proof certificate.
